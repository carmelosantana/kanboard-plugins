# SDD scratch (briefs, reports, review packages, progress ledger internals)
.superpowers/
# Docker/test scratch
testing/*.snapshot
testing/.env
# Kanboard source tree for host-side PHPUnit (large, not our code)
testing/kanboard-src/
# Hello Harmozi demo plugin — has its own git repo, pushed separately (never mounted in dev suite)
/HelloHarmozi/
# local-only: symlink to npx playwright cache for E2E scripts
/node_modules
